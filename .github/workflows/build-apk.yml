name: Build APK

on:
  push:
    paths:
      - "android/**"
      - ".github/workflows/build-apk.yml"
  pull_request:
    branches: [main]
  release:
    types: [published]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: android
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "17"

      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2
        with:
          gradle-version: 8.7
          cache-disabled: true

      - name: Build release APK
        env:
          ORG_GRADLE_PROJECT_cdnSupportsFailingBuildType: "true"
        run: |
          gradle :app:assembleRelease \
            -Dorg.gradle.jvmargs="-Xmx4096m -Dfile.encoding=UTF-8" \
            --no-daemon

      - name: Find APK
        run: find app/build/outputs -name "*.apk" -type f

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: absenku-apk
          path: android/app/build/outputs/apk/release/*.apk

      - name: Release APK (on GitHub Release)
        if: github.event_name == 'release'
        uses: shogo82148/actions-upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ github.event.release.upload_url }}
          asset_path: android/app/build/outputs/apk/release/*.apk
          asset_name: AbsenKu-v1.0.0.apk
          asset_content_type: application/vnd.android.package-archive
